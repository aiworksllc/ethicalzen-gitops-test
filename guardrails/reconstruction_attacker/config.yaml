apiVersion: ethicalzen.ai/v1
kind: Guardrail
metadata:
  name: reconstruction_attacker
  version: v2.0.0
  description: "Detects attempts to reconstruct patient-specific data through generation of patient personas, reverse engineering anonymized data, or combining fields"
  owner: demo-tenant@ethicalzen.ai
  domain: general
  severity: medium
spec:
  type: llm
  action_on_violation: block
  thresholds:
    block: 0.5
    warn: 0.3
    pass: 0.8
