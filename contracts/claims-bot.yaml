apiVersion: ethicalzen.ai/v1
kind: Contract
metadata:
  name: claims-bot-guardian
  version: v1.0.0
  description: "Guardrail contract for the Auto Claims Processing Bot"
  tenantId: acme
spec:
  serviceUrl: "https://internal-llm.acme.com/v1/chat/completions"
  guardrails:
    - pii_blocker
    - claims_approval_blocker
  failureModes:
    - pii_leak
    - unauthorized_approval
  validationMode: strict
